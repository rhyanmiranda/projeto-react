// esse arquivo é o bootstrap da aplicação, ponto de entrada, o primeiro arquivo a ser renderizado e fazendo isso em cascata

import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import App from './App'

const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
